<nav class="navbar navbar-expand-lg py-4 col-sm-12">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggle-icon"></span>
    </button>

    <div class="container-fluid col-11">
        {# Logo du site #}
        <div>
            <a href="{{ path('app_home') }}" class="navbar-brand">
                <img src="/images/images-1.jpeg" alt="logo du site">
            </a>
        </div>

        {# Filtre par date d'arrivée et de départ #}
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <form action="{{ path('by_date') }}" class="d-flex align-items-center">
                <label for="date_start" class="me-2">Arrivée :</label>
                <input type="date" id="date_start" name="date_start" class="form-control me-3" required>

                <label for="date_end" class="me-2">Départ :</label>
                <input type="date" id="date_end" name="date_end" class="form-control me-3" required>

                <button type="submit" class="btn btn-primary">Rechercher</button>
            </form>
        </div>

        {# Espace administrateur et utilisateur #}
        <div class="dropdown admin-menu">
            <a href="#" class="nav-link dropdown-toggle text-dark fs-5" role="button" data-bs-toggle="dropdown"
                aria-expanded="false" id="dropdownMenuLink">
                <i class="fa-regular fa-circle-user icon-user"></i>
            </a>
            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <a class="dropdown-item" href="#">Dashboard</a>
                    {% endif %}
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a class="dropdown-item" href="{{ path('app_login') }}">Connexion</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>