{% extends 'base.html.twig' %}

{% block title %}Equipements
{% endblock %}

{% block body %}
	<div class="game-list-container">
		<h1>{{ equipement.label }}</h1>

		<table class="game-table">
			<thead>
				<tr>
					<th>Id</th>
					<th>Label</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td>{{ equipement.id }}</td>
					<td>{{ equipement.label }}</td>
				</tr>
			</tbody>
		</table>

		<div class="action-buttons">
			<div>
				{# suppression #}
				{% include "equipement/_delete_form.html.twig" with {'button_class': 'btn btn-delete'} %}
			</div>
			<div>
				<a href="{{ path('app_equipement_edit' , {'id': equipement.id}) }}" class="btn btn-edit">Editer</a>


			</div>
			<div>
				<a href="{{path('app_equipement_index')}}" class="btn btn-back">

					<i class="fas fa-arrow-left"></i>Retour à la liste</a>

			</div>

		</div>

	{% endblock %}
